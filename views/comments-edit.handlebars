<!-- views/comments-edit.handlebars -->
<div class="row">
    <div class="col-sm-6 col-sm-offset-3">
        <form method="POST" action="/comments/{{comment._id}}?_method=PUT">
            {{> comments-form }}
            <!-- BUTTON -->
            <div class="form-group">
                <button class="btn btn-primary" type="submit">Save Review</button>
                <a href="/">Cancel</a>
            </div>
        </form>
    </div>
</div>